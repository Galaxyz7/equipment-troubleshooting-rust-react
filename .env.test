# Test Environment Configuration
# This file is used for running integration tests

# Test Database Configuration
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/equipment_troubleshooting_test

# JWT Configuration (use different secret for tests)
JWT_SECRET=test-secret-key-minimum-32-characters-for-testing-environment

# Admin Test Credentials
ADMIN_USERNAME=admin@example.com
ADMIN_PASSWORD_HASH=$argon2id$v=19$m=19456,t=2,p=1$test$hashedpassword

# Server Configuration
HOST=127.0.0.1
PORT=5001
FRONTEND_URL=http://localhost:5173

# Static Files (test build)
STATIC_FILES_PATH=../web/dist

# Test-specific settings
RUST_LOG=info
RUST_BACKTRACE=1
